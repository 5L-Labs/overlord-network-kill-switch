[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    unit: Unit tests with mocked dependencies
    integration: Integration tests requiring Pi-hole container
addopts = -v --tb=short
filterwarnings =
    ignore::DeprecationWarning
